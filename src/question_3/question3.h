#include <vector>

using std::vector;

bool test_config();

//In the header file, write a bool value return function prototype is_prime with an int parameter.
bool is_prime(int num);

//In header file, write a vector of int value return function prototype get_primes with an int num parameter.
vector<int> get_primes(int num);