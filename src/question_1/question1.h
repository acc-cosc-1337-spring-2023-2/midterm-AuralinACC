bool test_config();
//In the header file, create a vector of double value return function get_cookie_ingredients that accepts an int cookies to bake.
#include<vector>
#include<iostream>

using std:: vector;

vector<double> get_cookie_ingredients(int cookies);
